[instance]
eggs +=
    ZODB3
    RelStorage
    MySQL-python

rel-storage =
    type mysql
    db ${database:dbname}
    user ${database:dbuser}
    passwd ${database:dbpassword}
    host ${database:dbhost}
