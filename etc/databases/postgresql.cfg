[instance]
eggs +=
    ZODB3
    RelStorage
    psycopg2

rel-storage =
    type postgresql
    dbname ${database:dbname}
    user ${database:dbuser}
    password ${database:dbpassword}
    host ${database:dbhost}
